import CustomButton from './CustomButton';

function SearchButton() {
  return (
    <CustomButton toolTip="Search">
      <i class="bi bi-search"></i>
    </CustomButton>
  );
}

export default SearchButton;
