import CustomButton from './CustomButton';

function AddButton() {
  return (
    <CustomButton toolTip="Add">
      <i class="bi bi-plus-lg"></i>
    </CustomButton>
  );
}

export default AddButton;
